// the client code (aka.: other packages) should call cli.use('command', (args) => {
//  ...
// })
//
// this ensures the cli application can adapt to the global essence of this software
//
// the user should call "smite command ..." and that would call this args path
//
