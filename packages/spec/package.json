{
  "name": "@smite/spec",
  "version": "0.1.0",
  "description": "Deterministic compile-time projections over Smite HTTP descriptors: Express wiring spec and PlantUML diagrams.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist", "!dist/**/*.test.*", "!dist/.tsbuildinfo"],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run"
  },
  "sideEffects": false,
  "dependencies": {
    "@smite/core": "0.1.0"
  }
}
